1 - что лучше всего использовать для рендеринга страницы? autoDetectRenderer, CanvasRenderer, WebGLRenderer или по ситуации?

<!doctype html>
<html>
<head>
  <meta charset="utf-8">
  <title>Hello World</title>
</head>
<body>
  <script src="./pixi/pixi.4.3.2.js"></script>
  <script type="text/javascript">
    let type = "WebGL";
    if (!PIXI.utils.isWebGLSupported()) {
      type = "canvas";
    }

    let Container = PIXI.Container,
      autoDetectRenderer = PIXI.autoDetectRenderer,
      loader = PIXI.loader,
      resources = PIXI.loader.resources,
      Sprite = PIXI.Sprite,
      TextureCache = PIXI.utils.TextureCache,
      Graphics = PIXI.Graphics,
      Text = PIXI.Text,
      TilingSprite = PIXI.extras.TilingSprite;

    let stage = new Container(), 
      renderer = autoDetectRenderer(512, 512);
    
    document.body.appendChild(renderer.view);
    renderer.backgroundColor = 0xFFFFFF;

    loader
      .add('cat', './images/cat.png')
      .add(`explorer`,'./images/animals.png')
      .load(setup);
      let cat, explorer

    function setup() {
        let catTexture = TextureCache['cat']
        cat = new Sprite(catTexture)
        let animalsTexture = TextureCache['explorer']
        explorer = new Sprite(animalsTexture)
        stage.addChild(cat)
        cat.x = 64
        stage.addChild(explorer)
        cat.blendMode = PIXI.BLEND_MODES.SOFT_LIGHT;

        renderer.render(stage)
        gameLoop()
    }
    
    function gameLoop(){
        requestAnimationFrame(gameLoop)

    }
    
  </script>
</body>
</html>

Не работает св-во blend
