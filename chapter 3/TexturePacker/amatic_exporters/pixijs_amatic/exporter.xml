<exporter version="1.0">
    <!-- identifier of the exporter -->
    <name>amapixijs</name>

    <!-- for compatiblility only: allow referencing the exporter as "pixijs" -->
    <nameAlias>amapixijs</nameAlias>

    <!-- display name of the exporter for the combo box -->
    <displayName>PixiJS Amatic</displayName>
	
    <!-- description of the exporter -->
    <description>Exporter from Amatic for PixiJS</description>

    <!-- exporter version -->
    <version>1.0</version>
	
    <!-- currently only one file allowed - more to come with update -->
    <files>
        <file>
            <!-- name of this file variable -->
            <name>json</name>

            <!-- human readable name (for GUI) -->
            <displayName>Data file</displayName>

            <!-- file extension for the file -->
            <fileExtension>json</fileExtension>

            <!-- description what the file contains, used in tooltips in the GUI -->
            <description>JSON file for PixiJS</description>

            <!-- name of the template file -->
            <template>amapixijs.json</template>
        </file>
    </files>

    <!-- target framework supports trimming -->
    <supportsTrimming>true</supportsTrimming>

    <!-- target framework supports rotated sprites -->
    <supportsRotation>true</supportsRotation>

    <!-- rotated sprites direction (cw/ccw) -->
    <rotationDirection>cw</rotationDirection>

    <!-- supports npot sizes -->
    <supportsNPOT>true</supportsNPOT>

    <!-- supports file name stripping (remove .png etc) -->
    <supportsTrimSpriteNames>yes</supportsTrimSpriteNames>

    <!-- supports texure subpath -->
    <supportsTextureSubPath>yes</supportsTextureSubPath>

    <supportsPivotPoint>false</supportsPivotPoint>

    <supportsAnimations>false</supportsAnimations>
	
	<defaults>
        <extrude>0</extrude>
        <shapePadding>2</shapePadding>
        <borderPadding>2</borderPadding>
        <trimMode>Trim</trimMode>               <!-- None, Trim, Crop, CropKeepPos, Polygon -->
        <sizeConstraints>AnySize</sizeConstraints>     <!-- POT, WordAligned, AnySize -->
        <writePivotPoints>false</writePivotPoints>  <!-- true, false -->
    </defaults>
</exporter>
